global:
  resolve_timeout: 5m

route:
  receiver: default-log
  group_by:
    - alertname
    - job
    - instance
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: default-log

# Contoh integrasi tambahan:
# receivers:
#   - name: telegram
#     telegram_configs:
#       - bot_token: "CHANGE_ME"
#         chat_id: 123456789
#         send_resolved: true
#
#   - name: slack
#     slack_configs:
#       - api_url: "https://hooks.slack.com/services/CHANGE/ME"
#         channel: "#alerts"
#         send_resolved: true
